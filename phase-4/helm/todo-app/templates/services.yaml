{{/*
Generated by REAL GORDON MODE
Services
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-backend
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: todo-backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
  - port: {{ .Values.service.backend.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: todo-backend
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-chatbot
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: todo-chatbot
spec:
  type: {{ .Values.service.chatbot.type }}
  ports:
  - port: {{ .Values.service.chatbot.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: todo-chatbot
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app.fullname" . }}-frontend
  labels:
    {{- include "todo-app.labels" . | nindent 4 }}
    app: todo-frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
  - port: {{ .Values.service.frontend.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: todo-frontend
