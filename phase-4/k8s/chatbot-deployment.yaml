# Generated by REAL GORDON MODE
# Kubernetes Deployment for todo-chatbot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-chatbot
  labels:
    app: todo-chatbot
    version: gordon-v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-chatbot
  template:
    metadata:
      labels:
        app: todo-chatbot
        version: gordon-v1
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: todo-chatbot
        image: todo-chatbot:gordon-v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8001
          name: http
          protocol: TCP
        env:
        - name: BACKEND_API_URL
          value: "http://todo-backend:8000"
        - name: USE_QWEN_API
          value: "true"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 8001
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot
  labels:
    app: todo-chatbot
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: todo-chatbot
